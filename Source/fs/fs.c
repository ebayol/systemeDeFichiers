#include "fs.h"

int 
fs_format (const char *path, index_t nb_blocks, index_t size_block, index_t nb_inodes)
{
    return 0;
}

int 
fs_mount (fs_t * fs, const char *path, size_t size_cache)
{
    return 0;
}

int 
fs_umount (fs_t * fs)
{
    return 0;
}
